# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret - REQUIRED: Must be at least 32 characters, contain uppercase, lowercase, numbers, and special characters
# This secret is used to SIGN JWT tokens (not the token itself)
# Generate a new strong secret: openssl rand -base64 32
# Example (DO NOT use this in production - generate your own!):
JWT_SECRET=j0Va1afTfTmfFDXw91gD29EDKvN9ADQ/ZhfRt0hVWFo=

# JWT Expiration
# Access token - short-lived (used for API requests)
JWT_ACCESS_EXPIRY=15m
# Refresh token - long-lived (used to get new access tokens)
JWT_REFRESH_EXPIRY=7d
# Legacy support (will use JWT_ACCESS_EXPIRY if not set)
JWT_EXPIRES_IN=15m

# CORS - Frontend URL (required in production, restricts CORS to this origin only)
FRONTEND_URL=http://localhost:8080
ANDROID_EMULATOR_URL=http://10.0.2.2
